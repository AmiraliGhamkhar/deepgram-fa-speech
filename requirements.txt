# Pinned to explicitly tested versions/ranges (see README > Testing).
# CI (see .github/workflows/ci.yml) installs exactly these and runs the
# full test suite without a real Deepgram API key.

deepgram-sdk==7.9.0
python-dotenv>=1.0.1,<2.0
PyYAML>=6.0.1,<7.0
sounddevice>=0.4.6,<0.6
pyahocorasick>=2.0.0,<3.0
python-bidi>=0.4.2,<0.7

# Non-Windows clipboard/keystroke fallback only; the primary Windows path
# uses ctypes directly against user32/kernel32 and needs neither package.
pyautogui>=0.9.54,<0.10; sys_platform != "win32"
pyperclip>=1.8.2,<2.0; sys_platform != "win32"
